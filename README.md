# Pre-Investigation DFIR Agent (MVP)

Start with:
1. Install Docker & Docker Compose.
2. From repo root: `docker compose up --build`
3. POST a file to http://localhost:8000/ingest (use curl or Postman)
   Example:
4. Check `./data/artifacts/<case_id>/` for extracted files and metadata.
